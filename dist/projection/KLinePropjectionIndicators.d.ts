type Props = {
    symbolId: number;
    timeframe: number;
    selectedTime?: number;
    clearSelectedTime(): void;
};
export declare function KLinePropjectionIndicators({ symbolId, timeframe, selectedTime, clearSelectedTime, }: Props): any;
export {};
